// @flow

import React from 'react';
import {Redirect} from 'react-router';

import socket from '../../socket';

const CreateGame = () => {
  
}
  <Redirect to="/game/some-game"/>
);

export default CreateGame;
